default: &default
  adapter: postgresql
  pool: <%= ENV.fetch("RAILS_MAX_THREADS") { 5 } %>
  timeout: 5000

development:
  <<: *default
  database: covidinfo_development

test:
  <<: *default
  database: covidinfo_test

production:
  <<: *default
  database: covidinfo_production
